# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

Small C++ practice repository.

## Build & Run

```bash
g++ -o main test.cpp && ./main
```

No build system (CMake, Make, etc.) is configured. Compile directly with g++.
