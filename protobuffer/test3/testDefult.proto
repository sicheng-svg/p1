syntax = "proto3";
package testDefault;

// 在protobuf中，如果一个字段为设置值，则在序列化时，是不会被序列化在二进制序列中的。
// 这是为了极致的压缩空间。
// 但是这样可能导致二义性问题，发送方发送的值与默认值相同。那么接受方，是认为对方没有发送数据，还是说显示设置为默认值。
// 为了解决这个问题，建议使用optional关键字，被该关键字修饰后，会多一个has_xx的字段。用来检测是主动设置还是启用了默认值